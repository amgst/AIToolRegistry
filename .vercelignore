# Vercel ignore file - exclude local development files
database.sqlite
database.sqlite-shm
database.sqlite-wal
dist/index.js
# Note: server/ and shared/ are copied to api/ during build, so don't exclude them
# The copied versions in api/server/ and api/shared/ will be used by Vercel

